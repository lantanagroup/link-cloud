---

id: SubmitReport
name: Submit Report
version: 0.7.0
draft: true
summary: Instructs that a completed report be packaged for submission.
schemaPath: schema.json

---

## Overview

Sent when all patient data for a reporting period has been processed, prompting the packaging of the final submission bundle. This command is being extended to include pinned artifact and versioning information to support the [System State Manifest](/docs/custom/design/proposals/07-submission-system-state-enhancements).

<NodeGraph />

## Command Schema

### Key

```json
{
  "facilityId": "string",
  "startDate": "date-time",
  "endDate": "date-time"
}
```

### Headers

| Header Name      | Description                                                                                        |
|------------------|----------------------------------------------------------------------------------------------------|
| X-Correlation-Id | The correlation ID associated with the event that should be used in logs produced during the event |

### Payload

<SchemaViewer file="schema.json" />


 ## Raw Schema:schema.json

{
  "type": "object",
  "properties": {
    "patientIds": {
      "type": "array",
      "items": { "type": "string" }
    },
    "organization": {
      "type": "object"
    },
    "aggregates": {
      "type": "array",
      "items": { "type": "object" }
    },
    "measureIds": {
      "type": "array",
      "items": { "type": "string" }
    },
    "reportTrackingId": {
      "type": "string"
    },
    "systemState": {
      "type": "object",
      "properties": {
        "queryPlan": {
          "type": "object",
          "properties": {
            "id": { "type": "string" },
            "version": { "type": "string" }
          },
          "required": ["id", "version"]
        },
        "measures": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "version": { "type": "string" }
            },
            "required": ["id", "version"]
          }
        },
        "valueSets": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "version": { "type": "string" }
            },
            "required": ["id", "version"]
          }
        },
        "codeSystems": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "version": { "type": "string" }
            },
            "required": ["id", "version"]
          }
        },
        "validationProfiles": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "version": { "type": "string" }
            },
            "required": ["id", "version"]
          }
        },
        "conceptMaps": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "version": { "type": "string" }
            },
            "required": ["id", "version"]
          }
        },
        "normalizationSettings": {
          "type": "object",
          "description": "Normalization settings and enabled transforms"
        },
        "executionTimestamps": {
          "type": "object",
          "properties": {
            "start": { "type": "string", "format": "date-time" },
            "end": { "type": "string", "format": "date-time" }
          },
          "required": ["start", "end"]
        }
      },
      "required": ["queryPlan", "measures", "executionTimestamps"]
    }
  },
  "required": [
    "organization",
    "aggregates",
    "measureIds",
    "reportTrackingId",
    "systemState"
  ]
}
