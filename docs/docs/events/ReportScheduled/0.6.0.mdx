---
id: ReportScheduled
name: Report Scheduled
version: 0.6.0
schemaPath: schema.json
summary: Announces that a reporting period has started for a tenant.

---

## Overview

This message carries the reporting window and other configuration details so scheduling and reporting components
can persist the information and begin their respective workflows.

<NodeGraph />

## Event Schema

### Key

The facility ID (string) is used as the Kafka message key.

### Headers

| Header Name      | Description                                                                                        |
|------------------|----------------------------------------------------------------------------------------------------|
| X-Correlation-Id | The correlation ID associated with the event that should be used in logs produced during the event |

### Payload

<SchemaViewer file="schema.json" />

 ## Raw Schema:schema.json

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ReportScheduled",
  "type": "object",
  "properties": {
    "reportTypes": {
      "type": "array",
      "items": { "type": "string" }
    },
    "startDate": { "type": "string", "format": "date-time" },
    "endDate": { "type": ["string", "null"], "format": "date-time" },
    "frequency": { "type": "string" },
    "reportTrackingId": { "type": "string" }
  },
  "required": ["reportTypes", "startDate", "frequency", "reportTrackingId"],
  "additionalProperties": false
}