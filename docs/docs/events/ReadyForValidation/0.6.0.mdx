---

id: ReadyForValidation
name: Ready for Validation
version: 0.6.0
schemaPath: schema.json

summary: Notifies that a patient’s submission bundle is ready to be validated.

---

## Overview

Emitted after all patient resources have been stored, allowing the validation component to fetch the bundle and perform 
checks.

<NodeGraph />

## Event Schema

### Key

A JSON object containing the facility ID is used as the Kafka message key.

```json
{
  "facilityId": "string"
}
```

### Headers

| Header Name      | Description                                                                                        |
|------------------|----------------------------------------------------------------------------------------------------|
| X-Correlation-Id | The correlation ID associated with the event that should be used in logs produced during the event |

### Payload

<SchemaViewer file="schema.json" />

 ## Raw Schema:schema.json

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ReadyForValidation",
  "type": "object",
  "properties": {
    "patientId": { "type": "string" },
    "reportTrackingId": { "type": ["string", "null"] },
    "reportTypes": {
      "type": "array",
      "items": { "type": "string" }
    },
    "payloadUri": { "type": "string" }
  },
  "required": ["patientId", "payloadUri"],
  "additionalProperties": false
}
