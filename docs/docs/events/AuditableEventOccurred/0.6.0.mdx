---

id: AuditableEventOccurred
name: Auditable Event Occurred
version: 0.6.0
schemaPath: schema.json

summary: Captures an auditable action for security or compliance tracking.

---

## Overview

Emitted whenever important user or system activity happens so the audit service can maintain a comprehensive log.

<NodeGraph />

## Event Schema

### Key

The facility ID (string) is used as the Kafka message key.

### Headers

| Header Name      | Description                                                                                        |
|------------------|----------------------------------------------------------------------------------------------------|
| X-Correlation-Id | The correlation ID associated with the event that should be used in logs produced during the event |

### Payload

<SchemaViewer file="schema.json" />

 ## Raw Schema:schema.json

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AuditableEventOccurred",
  "type": "object",
  "properties": {
    "facilityId": { "type": ["string", "null"] },
    "serviceName": { "type": ["string", "null"] },
    "correlationId": { "type": ["string", "null"] },
    "eventDate": { "type": ["string", "null"], "format": "date-time" },
    "userId": { "type": ["string", "null"] },
    "user": { "type": ["string", "null"] },
    "action": {
      "type": ["string", "null"],
      "enum": ["Create", "Update", "Delete", "Query", "Submit", "Restore", null]
    },
    "resource": { "type": ["string", "null"] },
    "propertyChanges": {
      "type": ["array", "null"],
      "items": {
        "type": "object",
        "properties": {
          "propertyName": { "type": "string" },
          "initialPropertyValue": { "type": "string" },
          "newPropertyValue": { "type": "string" }
        }
      }
    },
    "notes": { "type": ["string", "null"] }
  }
}
