---
id: ReportStatusUpdated
name: "Report Status Updated"
version: 0.6.0
schemaPath: schema.json
summary: Emitted by various services to report the success or failure of specific reporting stages for a patient or a report.
draft: true

badges:
  - content: Proposed
    backgroundColor: blue
    textColor: black
---

## Overview

The `ReportStatusUpdated` event is a centralized mechanism for tracking the granular progress of a report. It is produced by processing services (Data Acquisition, Measure Evaluation, Validation, Submission) and consumed by the [[service|ReportService]] to update the state of reports and patients.

<NodeGraph />

## Event Schema

### Key

The facility ID (string) is used as the Kafka message key.

### Headers

| Header Name      | Description                                                                                        |
|------------------|----------------------------------------------------------------------------------------------------|
| X-Correlation-Id | The correlation ID associated with the event that should be used in logs produced during the event |

### Payload

<SchemaViewer file="schema.json" />


 ## Raw Schema:schema.json

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ReportStatusUpdated",
  "type": "object",
  "properties": {
    "FacilityId": {
      "type": "string",
      "description": "The unique identifier for the facility."
    },
    "ReportId": {
      "type": "string",
      "description": "The unique identifier for the report (e.g., ReportScheduleId)."
    },
    "PatientId": {
      "type": "string",
      "description": "The unique identifier for the patient (optional, for patient-specific updates)."
    },
    "Status": {
      "type": "string",
      "enum": [
        "InitialAcquisition",
        "InitialNormalization",
        "InitialEvaluation",
        "SupplementalAcquisition",
        "SupplementalNormalization",
        "SupplementalEvaluation",
        "Validation",
        "Submission",
        "CriticalFailure"
      ],
      "description": "The new status being reported."
    },
    "Service": {
      "type": "string",
      "description": "The name of the service reporting the status."
    },
    "IsSuccess": {
      "type": "boolean",
      "description": "Boolean indicating if the stage was completed successfully."
    },
    "ProcessingTime": {
      "type": "integer",
      "description": "The time taken (in milliseconds) to complete the reported phase."
    },
    "ErrorMessage": {
      "type": "string",
      "description": "Detailed error message if IsSuccess is false."
    },
    "CorrelationId": {
      "type": "string",
      "description": "For tracing the request across services."
    }
  },
  "required": [
    "FacilityId",
    "ReportId",
    "Status",
    "Service",
    "IsSuccess",
    "ProcessingTime",
    "CorrelationId"
  ]
}
