resources:
  pipelines:
  - pipeline: pipeline-trigger
    source: Deploy_All_Services
    trigger: true

trigger:
  branches:
    include:
    - dev
    - release/*
    - linkathon/*
  paths:
    include:
    - DotNet/Account/*
    - DotNet/Shared/*
    exclude:
    - '*'
    
pr: none

pool:
  vmImage: 'ubuntu-latest'

variables:
- group: link-cloud-variables
- name: project
  value: 'DotNet/Account/Account.csproj'
- name: testProject
  value: 'DotNet/AccountUnitTests/AccountUnitTests.csproj'
- name: serviceName
  value: 'Account'
- name: registry-repo-Name
  value: 'link-account'
- name: dockerPath
  value: '**/DotNet/Account/Dockerfile'
- name: containerRegistry
  value: 'NHSNLink ACR Premium'
- name: colon
  value: ': '
  
steps:

  - task: Bash@3
    inputs:
      targetType: 'inline'
      script: |
        GIT_COMMIT=$(git rev-parse --short HEAD)
        echo "GIT_COMMIT: ${GIT_COMMIT}"
        echo "##vso[task.setvariable variable=GIT_COMMIT]${GIT_COMMIT}"

#   - task: DotNetCoreCLI@2
#     displayName: Restore
#     inputs:
#       command: restore
#       projects: '$(project)'
  
#   - task: DotNetCoreCLI@2
#     displayName: Build
#     inputs:
#       command: build
#       projects: '$(project)'

#   - task: DotNetCoreCLI@2
#     inputs:
#       command: 'test'
#       projects: $(testProject)'
#     displayName: 'Run Tests'

  - task: PowerShell@2
    inputs:
      targetType: 'inline'
      script: |
        echo BSD
        echo $(Build.SourcesDirectory)
        ls
        # echo $(Build.ArtifactStagingDirectory)
        # ls $(Build.ArtifactStagingDirectory)
        # echo "Pipeline.Workspace"
        # echo $(Pipeline.Workspace)
        # ls $(Pipeline.Workspace)
        echo CD
        cd "/home/vsts/work/1/s"
        ls
        $old = get-content -Path "/home/vsts/work/1/s/DotNet/Account/appsettings.json" | select-string -pattern "GitComm"
        $gitcommit = '"GitCommit"'

        $plus2 = '"${GIT_COMMIT}"'
        $new = $gitcommit + ${colon} + $plus2
        (Get-Content -Path "/home/vsts/work/1/s/DotNet/Account/appsettings.json") | ForEach-Object { $_ -replace ${old}, ${new} } | Set-Content -Path "/home/vsts/work/1/s/DotNet/Account/appsettings.json"
        
        echo "Getting Contents"
        Get-Content -Path "/home/vsts/work/1/s/DotNet/Account/appsettings.json

#   - task: PowerShell@2
#     inputs:
#       targetType: 'inline'
#       script: |
#         if ("$(Build.SourceBranch)" -like "*release/*") {
#         $myTag1 = "release-$(Build.SourceBranchName)-$(GIT_COMMIT)"
#         }
#         else { 
#         $myTag1 = "$(Build.SourceBranchName)-$(GIT_COMMIT)"
#         }
#         Write-Host "##vso[task.setvariable variable=MyTag]$myTag1"
#         Write-Host "Set MyTag to: $myTag1"

#   - task: Docker@2
#     displayName: "Build & Push Account Docker Image"
#     condition: always()
#     inputs:
#       containerRegistry: $(containerRegistry)
#       repository: $(registry-repo-name)
#       command: 'buildAndPush'
#       Dockerfile: $(dockerPath)
#       tags: |
#         latest
#         $(MyTag)
#       buildContext: '$(Build.Repository.LocalPath)'

#   - task: PublishPipelineArtifact@1
#     inputs:
#       targetPath: '$(Build.ArtifactStagingDirectory)'
#       artifact: 'manifest'
