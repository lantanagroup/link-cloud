spring:
  cloud:
    azure:
      appconfiguration:
        enabled: false
        stores:
          - endpoint: ${AZURE_APP_CONFIG_ENDPOINT}
            selects:
              - key-filter: '/'
                label-filter: ',Validation'
