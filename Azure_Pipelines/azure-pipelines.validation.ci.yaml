trigger:
  paths:
    include:
    - Java/validation/*

pool:
  vmImage: 'ubuntu-latest'

steps:
  - task: Maven@2
    displayName: Maven Install
    env:
      SHARED_BOTW_FEED_TOKEN: $(SHARED_BOTW_FEED_TOKEN)
    inputs:
      mavenPomFile: 'Java/validation/pom.xml'
      goals: install
      options: '-s .m2/settings.xml'