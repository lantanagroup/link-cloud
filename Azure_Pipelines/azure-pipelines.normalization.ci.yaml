trigger:
  paths:
    include:
    - DotNet/Normalization/*

pool:
  vmImage: 'ubuntu-latest'

variables:
  project: 'DotNet/Normalization/Normalization.csproj'
  testProject: 'DotNet/NormalizationUnitTests/NormalizationUnitTests.csproj'
  nugetPath: 'DotNet/Normalization/nuget.config'
  serviceName: 'Normalization'

steps:
  - template: ../azure-nuget-package-source.yml

  - task: DotNetCoreCLI@2
    displayName: Restore
    inputs:
      command: restore
      projects: '$(project)'
      nugetConfigPath: '$(nugetPath)'
      feedsToUse: config
  
  - task: DotNetCoreCLI@2
    displayName: Build
    inputs:
      command: build
      projects: '$(project)'

  - task: DotNetCoreCLI@2
    inputs:
      command: 'test'
      projects: $(testProject)
    displayName: 'Run Tests'

