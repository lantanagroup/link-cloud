trigger:
  branches:
    include:
    - release/*
    #- dev
  paths:
    include:
    - DotNet/*/Migrations
    - Java/validation/src/main/resources/database/migrations
    exclude:
    - '*'
    
pr: none

pool:
  vmImage: 'ubuntu-latest'

variables:
- group: 'MSSQL_DBCreation'

parameters:
- name: previous_release
  displayName: "What is the Tag or Commit SHA of the Previous Release? (i.e. : v0.2.0-beta.1)"
  type: string

- name: current_release
  displayName: "What is the Tag or Commit SHA of the Current Release? (i.e. : v0.2.0-beta.2)"
  type: string

#####################

stages:
- stage: Build
  jobs:
  - job: getChangedFiles
    displayName: Get Changes
    steps: 
      - checkout: self
        fetchDepth: 0
        persistCredentials: true
      - task: PowerShell@2
        name: setVariables
        inputs:
          targetType: 'inline'
          script: |
            Write-Host ${env:BYPASSCHANGEEVALUATION}
            
            # Get any changed files
            $prev = '${{ parameters.previous_release }}'
            $curr = '${{ parameters.current_release }}'

            # Define path to changedServices.txt
            $changedFilesFile = "$(Build.SourcesDirectory)/changedfiles.txt"
            $changedServicesFile = "$(Build.SourcesDirectory)/changedServices.txt"

            Write-Host "Previous Release: $prev"
            Write-Host "Current Release: $curr"

            git fetch --tags

            If ($prev && $curr) {
              $changedFiles = git diff $prev $curr --name-only
            } else {
              $changedFiles = git diff --name-status HEAD HEAD^
            }
            
            Write-Host "Changed files:"
            Write-Host $changedFiles

            # Save output to changedfiles.txt
            $changedFiles | Out-File -FilePath $changedFilesFile -Encoding utf8

            # List of DotNet services to check
            $dotnetServices = @("Account", "Audit", "Census", "Normalization", "QueryDispatch", "Report", "Submission", "Tenant" ) 
            $javaServices = @("validation" )
            $dataAcquisition = @("DataAcquisition" )

            # DotNet Services
            foreach ($service in $dotnetServices) {
              $path = "Dotnet/$service/migrations"
              $match = $changedFiles | Where-Object { $_ -like "$path*" }

              if ($match) {
                $message = "$service migration files have changed."
                Write-Host $message
                Add-Content -Path $changedServicesFile -Value $message
              } else {
                Write-Host "No changes in $service files."
                }
              }

            # dataAcquisition Services
            foreach ($service in $dataAcquisition) {
              $path = "Dotnet/$service.Domain/Migrations"
              $match = $changedFiles | Where-Object { $_ -like "$path*" }

              if ($match) {
                $message = "$service migration files have changed."
                Write-Host $message
                Add-Content -Path $changedServicesFile -Value $message
              } else {
                Write-Host "No changes in $service files."
                }
              }

            # Java Services
            foreach ($service in $javaServices) {
              $path = "Java/$service/src/main/resources/database/migrations"
              $match = $changedFiles | Where-Object { $_ -like "$path*" }

              if ($match) {
                $message = "$service migration files have changed."
                Write-Host $message
                Add-Content -Path $changedServicesFile -Value $message
              } else {
                Write-Host "No changes in $service files."
                }
              }

            Write-Host "##vso[task.setvariable variable=task.A.status]Success"

      # Publish Changed Services

      - task: PublishBuildArtifacts@1
        inputs:
          PathtoPublish: '$(Build.SourcesDirectory)/changedServices.txt'
          ArtifactName: 'changedServices'

      - task: PublishBuildArtifacts@1
        inputs:
          PathtoPublish: '$(Build.SourcesDirectory)/changedfiles.txt'
          ArtifactName: 'changedFiles'

############################################################################################
# SQL Script Generation
############################################################################################
  # Account
  - job: Account
    displayName: "Generate Account Schema Script"
    steps:
      - script: |
          cd DotNet/Account
          dotnet new tool-manifest --force
          dotnet tool install dotnet-ef --version 8.0.22
          dotnet tool restore
          dotnet tool run dotnet-ef --version
        displayName: Install dotnet-ef (local pinned)
      - script: |
          cd DotNet/Account
          mkdir -p $(build.artifactstagingdirectory)/DatabaseScripts
          dotnet tool run dotnet-ef migrations script --idempotent --verbose -c AccountDbContext -s $(build.sourcesdirectory)/DotNet/Account/Account.csproj -p $(build.sourcesdirectory)/DotNet/Account/Account.csproj -o $(build.artifactstagingdirectory)/DatabaseScripts/accountDb.sql
        displayName: Generate Account SQL Script
      - task: PublishBuildArtifacts@1
        inputs:
         PathtoPublish: '$(Build.ArtifactStagingDirectory)/DatabaseScripts/accountDb.sql'
         ArtifactName: 'account-db'
         publishLocation: 'container'
        displayName: Publish Account SQL Script  
  ###
  # Audit
  - job: Audit
    displayName: "Generate Audit Schema Script"
    steps:
      - script: |
          cd DotNet/Audit
          dotnet new tool-manifest --force
          dotnet tool install dotnet-ef --version 8.0.22
          dotnet tool restore
          dotnet tool run dotnet-ef --version
        displayName: Install dotnet-ef (local pinned)
      - script: |
          cd DotNet/Audit
          mkdir -p $(build.artifactstagingdirectory)/DatabaseScripts
          dotnet tool run dotnet-ef migrations script --idempotent --verbose -c AuditDbContext -s $(build.sourcesdirectory)/DotNet/Audit/Audit.csproj -p $(build.sourcesdirectory)/DotNet/Audit/Audit.csproj -o $(build.artifactstagingdirectory)/DatabaseScripts/auditDb.sql
        displayName: Generate Audit SQL Script
      - task: PublishBuildArtifacts@1
        inputs:
         PathtoPublish: '$(Build.ArtifactStagingDirectory)/DatabaseScripts/auditDb.sql'
         ArtifactName: 'audit-db'
         publishLocation: 'container'
        displayName: Publish Audit SQL Script  
  ###
  # Census
  - job: Census
    displayName: "Generate Census Schema Script"
    steps:
      - script: |
          cd DotNet/Census
          dotnet new tool-manifest --force
          dotnet tool install dotnet-ef --version 8.0.22
          dotnet tool restore
          dotnet tool run dotnet-ef --version
        displayName: Install dotnet-ef (local pinned)
      - script: |
          cd DotNet/Census
          mkdir -p $(build.artifactstagingdirectory)/DatabaseScripts
          dotnet tool run dotnet-ef migrations script --idempotent --verbose -c CensusContext -s $(build.sourcesdirectory)/DotNet/Census/Census.csproj -p $(build.sourcesdirectory)/DotNet/Census/Census.csproj -o $(build.artifactstagingdirectory)/DatabaseScripts/censusDb.sql
        displayName: Generate Census SQL Script
      - task: PublishBuildArtifacts@1
        inputs:
         PathtoPublish: '$(Build.ArtifactStagingDirectory)/DatabaseScripts/censusDb.sql'
         ArtifactName: 'census-db'
         publishLocation: 'container'
        displayName: Publish Census SQL Script  
  ###
  # DataAcquisition
  - job: DataAcquisition
    displayName: "Generate Data Acquisition Schema Script"
    steps:
      - script: |
          cd DotNet/DataAcquisition
          dotnet new tool-manifest --force
          dotnet tool install dotnet-ef --version 8.0.22
          dotnet tool restore
          dotnet tool run dotnet-ef --version
        displayName: Install dotnet-ef (local pinned)
      - script: |
          cd DotNet/DataAcquisition
          mkdir -p $(build.artifactstagingdirectory)/DatabaseScripts
          dotnet tool run dotnet-ef migrations script --idempotent --verbose -c DataAcquisitionDbContext -s $(build.sourcesdirectory)/DotNet/DataAcquisition/DataAcquisition.csproj -p $(build.sourcesdirectory)/DotNet/DataAcquisition.Domain/DataAcquisition.Domain.csproj -o $(build.artifactstagingdirectory)/DatabaseScripts/dataDb.sql
        displayName: Generate DataAcquisition SQL Script
      - task: PublishBuildArtifacts@1
        inputs:
         PathtoPublish: '$(Build.ArtifactStagingDirectory)/DatabaseScripts/dataDb.sql'
         ArtifactName: 'data-db'
         publishLocation: 'container'
        displayName: Publish Data Acquisition SQL Script  
  ###
  # Normalization
  - job: Normalization
    displayName: "Generate Normalization Schema Script"
    steps:
      - script: |
          cd DotNet/Normalization
          dotnet new tool-manifest --force
          dotnet tool install dotnet-ef --version 8.0.22
          dotnet tool restore
          dotnet tool run dotnet-ef --version
        displayName: Install dotnet-ef (local pinned)
      - script: |
          cd DotNet/Normalization
          mkdir -p $(build.artifactstagingdirectory)/DatabaseScripts
          dotnet tool run dotnet-ef migrations script --idempotent --verbose -c NormalizationDbContext -s $(build.sourcesdirectory)/DotNet/Normalization/Normalization.csproj -p $(build.sourcesdirectory)/DotNet/Normalization/Normalization.csproj -o $(build.artifactstagingdirectory)/DatabaseScripts/normalizationDb.sql
        displayName: Generate Normalization SQL Script
      - task: PublishBuildArtifacts@1
        inputs:
         PathtoPublish: '$(Build.ArtifactStagingDirectory)/DatabaseScripts/normalizationDb.sql'
         ArtifactName: 'normalization-db'
         publishLocation: 'container'
        displayName: Publish Normalization SQL Script  
  ###
  # QueryDispatch
  - job: QueryDispatch
    displayName: "Generate QueryDispatch Schema Script"
    steps:
      - script: |
          cd DotNet/QueryDispatch
          dotnet new tool-manifest --force
          dotnet tool install dotnet-ef --version 8.0.22
          dotnet tool restore
          dotnet tool run dotnet-ef --version
        displayName: Install dotnet-ef (local pinned)
      - script: |
          cd DotNet/QueryDispatch
          mkdir -p $(build.artifactstagingdirectory)/DatabaseScripts
          dotnet tool run dotnet-ef migrations script --idempotent --verbose -c QueryDispatchDbContext -s $(build.sourcesdirectory)/DotNet/QueryDispatch/QueryDispatch.csproj -p $(build.sourcesdirectory)/DotNet/QueryDispatch/QueryDispatch.csproj -o $(build.artifactstagingdirectory)/DatabaseScripts/querydispatchDb.sql
        displayName: Generate QueryDispatch SQL Script
      - task: PublishBuildArtifacts@1
        inputs:
         PathtoPublish: '$(Build.ArtifactStagingDirectory)/DatabaseScripts/querydispatchDb.sql'
         ArtifactName: 'querydispatch-db'
         publishLocation: 'container'
        displayName: Publish Query Dispatch SQL Script
  ###
  # Report
  - job: Report
    displayName: "Generate Report Schema Script"
    steps:
      - script: |
          cd DotNet/Report
          dotnet new tool-manifest --force
          dotnet tool install dotnet-ef --version 8.0.22
          dotnet tool restore
          dotnet tool run dotnet-ef --version
        displayName: Install dotnet-ef (local pinned)
      - script: |
          cd DotNet/Report
          mkdir -p $(build.artifactstagingdirectory)/DatabaseScripts
          dotnet tool run dotnet-ef migrations script --idempotent --verbose -c ReportDbContext -s $(build.sourcesdirectory)/DotNet/Report/Report.csproj -p $(build.sourcesdirectory)/DotNet/Report/Report.csproj -o $(build.artifactstagingdirectory)/DatabaseScripts/ReportDb.sql
        displayName: Generate Report SQL Script
      - task: PublishBuildArtifacts@1
        inputs:
         PathtoPublish: '$(Build.ArtifactStagingDirectory)/DatabaseScripts/ReportDb.sql'
         ArtifactName: 'Report-db'
         publishLocation: 'container'
        displayName: Publish Query Dispatch SQL Script  
  ###
  # Submission
  - job: Submission
    displayName: "Generate Submission Schema Script"
    steps:
      - script: |
          cd DotNet/Submission
          dotnet new tool-manifest --force
          dotnet tool install dotnet-ef --version 8.0.22
          dotnet tool restore
          dotnet tool run dotnet-ef --version
        displayName: Install dotnet-ef (local pinned)
      - script: |
          cd DotNet/Submission
          mkdir -p $(build.artifactstagingdirectory)/DatabaseScripts
          dotnet tool run dotnet-ef migrations script --idempotent --verbose -c SubmissionContext -s $(build.sourcesdirectory)/DotNet/Submission/Submission.csproj -p $(build.sourcesdirectory)/DotNet/Submission.Data/Submission.Data.csproj -o $(build.artifactstagingdirectory)/DatabaseScripts/submissionDb.sql
        displayName: Generate Submission SQL Script
      - task: PublishBuildArtifacts@1
        inputs:
         PathtoPublish: '$(Build.ArtifactStagingDirectory)/DatabaseScripts/submissionDb.sql'
         ArtifactName: 'submission-db'
         publishLocation: 'container'
        displayName: Publish Submission SQL Script  
  ###
  # Tenant
  - job: Tenant
    displayName: "Generate Tenant Schema Script"
    steps:
      - script: |
          cd DotNet/Tenant
          dotnet new tool-manifest --force
          dotnet tool install dotnet-ef --version 8.0.22
          dotnet tool restore
          dotnet tool run dotnet-ef --version
        displayName: Install dotnet-ef (local pinned)
      - script: |
          cd DotNet/Tenant
          mkdir -p $(build.artifactstagingdirectory)/DatabaseScripts
          dotnet tool run dotnet-ef migrations script --idempotent --verbose -c TenantDbContext -s $(build.sourcesdirectory)/DotNet/Tenant/Tenant.csproj -p $(build.sourcesdirectory)/DotNet/Tenant/Tenant.csproj -o $(build.artifactstagingdirectory)/DatabaseScripts/tenantDb.sql
        displayName: Generate Tenant SQL Script
      - task: PublishBuildArtifacts@1
        inputs:
         PathtoPublish: '$(Build.ArtifactStagingDirectory)/DatabaseScripts/tenantDb.sql'
         ArtifactName: 'tenant-db'
         publishLocation: 'container'
        displayName: Publish Tenant SQL Script  
    ###
  # Validation
  - job: Validation
    displayName: "Generate Validation Schema Script"
    steps:
    - task: CopyFiles@2
      displayName: Copy Validation SQL Scripts to Artifact Staging Directory
      inputs:
       SourceFolder: '$(build.sourcesdirectory)/Java/validation/src/main/resources/database/migrations/'
       TargetFolder: '$(Build.ArtifactStagingDirectory)/DatabaseScripts'
       Contents: 'V*.sql'
    - task: PublishBuildArtifacts@1
      inputs:
       PathtoPublish: '$(Build.ArtifactStagingDirectory)/DatabaseScripts'
       ArtifactName: 'validation-db'
       publishLocation: 'container'
      displayName: Publish Validation SQL Script  
  ###

############################################################################################
# SQL Script Apply TEST
############################################################################################
- stage: Apply_TEST_Update_SQL_DBs
  trigger: manual
  displayName: Apply_TEST_Update_SQL_DBs
  condition: always()

  # Requires Windows runner for SQL
  pool:
    vmImage: 'windows-latest'

  jobs:
  ### Account
  - job: UpdateAccountSchema
    displayName: Update Account DB Schema
    steps:
      - download: current
        artifact: account-db

      - task: SqlAzureDacpacDeployment@1
        displayName: Apply Account DB Schema
        inputs:
          azureSubscription: "NHSNLink Resource Manager"
          ServerName: '$(link-db_server)'
          DatabaseName: 'link-test-botw-account'
          SqlUsername: '$(link-db_username)'
          SqlPassword: '$(link-db_pass)'
          deployType: 'SqlTask'
          SqlFile: '$(Pipeline.Workspace)/account-db/accountDb.sql'

  ### Audit
  - job: UpdateauditSchema
    displayName: Update audit DB Schema
    steps:
      - download: current
        artifact: audit-db

      - task: SqlAzureDacpacDeployment@1
        displayName: Apply audit DB Schema
        inputs:
          azureSubscription: "NHSNLink Resource Manager"
          ServerName: '$(link-db_server)'
          DatabaseName: 'link-test-botw-audit'
          SqlUsername: '$(link-db_username)'
          SqlPassword: '$(link-db_pass)'
          deployType: 'SqlTask'
          SqlFile: '$(Pipeline.Workspace)/audit-db/auditDb.sql'

  ### Census
  - job: UpdatecensusSchema
    displayName: Update census DB Schema
    steps:
      - download: current
        artifact: census-db

      - task: SqlAzureDacpacDeployment@1
        displayName: Apply census DB Schema
        inputs:
          azureSubscription: "NHSNLink Resource Manager"
          ServerName: '$(link-db_server)'
          DatabaseName: 'link-test-botw-census'
          SqlUsername: '$(link-db_username)'
          SqlPassword: '$(link-db_pass)'
          deployType: 'SqlTask'
          SqlFile: '$(Pipeline.Workspace)/census-db/censusDb.sql'

  ### Data
  - job: UpdatedataSchema
    displayName: Update data DB Schema
    steps:
      - download: current
        artifact: data-db

      - task: SqlAzureDacpacDeployment@1
        displayName: Apply data DB Schema
        inputs:
          azureSubscription: "NHSNLink Resource Manager"
          ServerName: '$(link-db_server)'
          DatabaseName: 'link-test-botw-data'
          SqlUsername: '$(link-db_username)'
          SqlPassword: '$(link-db_pass)'
          deployType: 'SqlTask'
          SqlFile: '$(Pipeline.Workspace)/data-db/dataDb.sql'

  ### Normalization
  - job: UpdatenormalizationSchema
    displayName: Update normalization DB Schema
    steps:
      - download: current
        artifact: normalization-db

      - task: SqlAzureDacpacDeployment@1
        displayName: Apply normalization DB Schema
        inputs:
          azureSubscription: "NHSNLink Resource Manager"
          ServerName: '$(link-db_server)'
          DatabaseName: 'link-test-botw-normalization'
          SqlUsername: '$(link-db_username)'
          SqlPassword: '$(link-db_pass)'
          deployType: 'SqlTask'
          SqlFile: '$(Pipeline.Workspace)/normalization-db/normalizationDb.sql'

  ### QueryDispatch
  - job: UpdatequerydispatchSchema
    displayName: Update querydispatch DB Schema
    steps:
      - download: current
        artifact: querydispatch-db

      - task: SqlAzureDacpacDeployment@1
        displayName: Apply querydispatch DB Schema
        inputs:
          azureSubscription: "NHSNLink Resource Manager"
          ServerName: '$(link-db_server)'
          DatabaseName: 'link-test-botw-querydispatch'
          SqlUsername: '$(link-db_username)'
          SqlPassword: '$(link-db_pass)'
          deployType: 'SqlTask'
          SqlFile: '$(Pipeline.Workspace)/querydispatch-db/querydispatchDb.sql'

  ### Report
  - job: UpdatereportSchema
    displayName: Update report DB Schema
    steps:
      - download: current
        artifact: report-db

      - task: SqlAzureDacpacDeployment@1
        displayName: Apply report DB Schema
        inputs:
          azureSubscription: "NHSNLink Resource Manager"
          ServerName: '$(link-db_server)'
          DatabaseName: 'link-test-botw-report'
          SqlUsername: '$(link-db_username)'
          SqlPassword: '$(link-db_pass)'
          deployType: 'SqlTask'
          SqlFile: '$(Pipeline.Workspace)/report-db/reportDb.sql'

  ### Submission
  - job: UpdatesubmissionSchema
    displayName: Update submission DB Schema
    steps:
      - download: current
        artifact: submission-db

      - task: SqlAzureDacpacDeployment@1
        displayName: Apply submission DB Schema
        inputs:
          azureSubscription: "NHSNLink Resource Manager"
          ServerName: '$(link-db_server)'
          DatabaseName: 'link-test-botw-submission'
          SqlUsername: '$(link-db_username)'
          SqlPassword: '$(link-db_pass)'
          deployType: 'SqlTask'
          SqlFile: '$(Pipeline.Workspace)/submission-db/submissionDb.sql'

  ### Tenant
  - job: UpdatetenantSchema
    displayName: Update tenant DB Schema
    steps:
      - download: current
        artifact: tenant-db

      - task: SqlAzureDacpacDeployment@1
        displayName: Apply tenant DB Schema
        inputs:
          azureSubscription: "NHSNLink Resource Manager"
          ServerName: '$(link-db_server)'
          DatabaseName: 'link-test-botw-tenant'
          SqlUsername: '$(link-db_username)'
          SqlPassword: '$(link-db_pass)'
          deployType: 'SqlTask'
          SqlFile: '$(Pipeline.Workspace)/tenant-db/tenantDb.sql'

  ### Validation
  - job: UpdatevalidationSchema
    displayName: Update validation DB Schema
    steps:
      - download: current
        artifact: validation-db

      - task: SqlAzureDacpacDeployment@1
        displayName: Apply validation DB Schema
        inputs:
          azureSubscription: "NHSNLink Resource Manager"
          ServerName: '$(link-db_server)'
          DatabaseName: 'link-test-botw-validation'
          SqlUsername: '$(link-db_username)'
          SqlPassword: '$(link-db_pass)'
          deployType: 'SqlTask'
          SqlFile: '$(Pipeline.Workspace)/validation-db/V20241101__Initial_schema.sql'

############################################################################################
# SQL Script Apply QA
############################################################################################
- stage: Apply_QA_Update_SQL_DBs
  trigger: manual
  displayName: Apply_QA_Update_SQL_DBs
  # dependsOn: Build
  condition: always()

  # Requires Windows runner for SQL
  pool:
    vmImage: 'windows-latest'

  jobs:
  ### Account
  - job: UpdateAccountSchema
    displayName: Update Account DB Schema
    steps:
      - download: current
        artifact: account-db

      - task: SqlAzureDacpacDeployment@1
        displayName: Apply Account DB Schema
        inputs:
          azureSubscription: "NHSNLink Resource Manager"
          ServerName: '$(link-db_server)'
          DatabaseName: 'link-qa-botw-account'
          SqlUsername: '$(link-db_username)'
          SqlPassword: '$(link-db_pass)'
          deployType: 'SqlTask'
          SqlFile: '$(Pipeline.Workspace)/account-db/accountDb.sql'

  ### Audit
  - job: UpdateauditSchema
    displayName: Update audit DB Schema
    steps:
      - download: current
        artifact: audit-db

      - task: SqlAzureDacpacDeployment@1
        displayName: Apply audit DB Schema
        inputs:
          azureSubscription: "NHSNLink Resource Manager"
          ServerName: '$(link-db_server)'
          DatabaseName: 'link-qa-botw-audit'
          SqlUsername: '$(link-db_username)'
          SqlPassword: '$(link-db_pass)'
          deployType: 'SqlTask'
          SqlFile: '$(Pipeline.Workspace)/audit-db/auditDb.sql'

  ### Census
  - job: UpdatecensusSchema
    displayName: Update census DB Schema
    steps:
      - download: current
        artifact: census-db

      - task: SqlAzureDacpacDeployment@1
        displayName: Apply census DB Schema
        inputs:
          azureSubscription: "NHSNLink Resource Manager"
          ServerName: '$(link-db_server)'
          DatabaseName: 'link-qa-botw-census'
          SqlUsername: '$(link-db_username)'
          SqlPassword: '$(link-db_pass)'
          deployType: 'SqlTask'
          SqlFile: '$(Pipeline.Workspace)/census-db/censusDb.sql'

  ### Data
  - job: UpdatedataSchema
    displayName: Update data DB Schema
    steps:
      - download: current
        artifact: data-db

      - task: SqlAzureDacpacDeployment@1
        displayName: Apply data DB Schema
        inputs:
          azureSubscription: "NHSNLink Resource Manager"
          ServerName: '$(link-db_server)'
          DatabaseName: 'link-qa-botw-data'
          SqlUsername: '$(link-db_username)'
          SqlPassword: '$(link-db_pass)'
          deployType: 'SqlTask'
          SqlFile: '$(Pipeline.Workspace)/data-db/dataDb.sql'

  ### Normalization
  - job: UpdatenormalizationSchema
    displayName: Update normalization DB Schema
    steps:
      - download: current
        artifact: normalization-db

      - task: SqlAzureDacpacDeployment@1
        displayName: Apply normalization DB Schema
        inputs:
          azureSubscription: "NHSNLink Resource Manager"
          ServerName: '$(link-db_server)'
          DatabaseName: 'link-qa-botw-normalization'
          SqlUsername: '$(link-db_username)'
          SqlPassword: '$(link-db_pass)'
          deployType: 'SqlTask'
          SqlFile: '$(Pipeline.Workspace)/normalization-db/normalizationDb.sql'

  ### QueryDispatch
  - job: UpdatequerydispatchSchema
    displayName: Update querydispatch DB Schema
    steps:
      - download: current
        artifact: querydispatch-db

      - task: SqlAzureDacpacDeployment@1
        displayName: Apply querydispatch DB Schema
        inputs:
          azureSubscription: "NHSNLink Resource Manager"
          ServerName: '$(link-db_server)'
          DatabaseName: 'link-qa-botw-querydispatch'
          SqlUsername: '$(link-db_username)'
          SqlPassword: '$(link-db_pass)'
          deployType: 'SqlTask'
          SqlFile: '$(Pipeline.Workspace)/querydispatch-db/querydispatchDb.sql'

  ### Report
  - job: UpdatereportSchema
    displayName: Update report DB Schema
    steps:
      - download: current
        artifact: report-db

      - task: SqlAzureDacpacDeployment@1
        displayName: Apply report DB Schema
        inputs:
          azureSubscription: "NHSNLink Resource Manager"
          ServerName: '$(link-db_server)'
          DatabaseName: 'link-qa-botw-report'
          SqlUsername: '$(link-db_username)'
          SqlPassword: '$(link-db_pass)'
          deployType: 'SqlTask'
          SqlFile: '$(Pipeline.Workspace)/report-db/reportDb.sql'

  ### Submission
  - job: UpdatesubmissionSchema
    displayName: Update submission DB Schema
    steps:
      - download: current
        artifact: submission-db

      - task: SqlAzureDacpacDeployment@1
        displayName: Apply submission DB Schema
        inputs:
          azureSubscription: "NHSNLink Resource Manager"
          ServerName: '$(link-db_server)'
          DatabaseName: 'link-qa-botw-submission'
          SqlUsername: '$(link-db_username)'
          SqlPassword: '$(link-db_pass)'
          deployType: 'SqlTask'
          SqlFile: '$(Pipeline.Workspace)/submission-db/submissionDb.sql'

  ### Tenant
  - job: UpdatetenantSchema
    displayName: Update tenant DB Schema
    steps:
      - download: current
        artifact: tenant-db

      - task: SqlAzureDacpacDeployment@1
        displayName: Apply tenant DB Schema
        inputs:
          azureSubscription: "NHSNLink Resource Manager"
          ServerName: '$(link-db_server)'
          DatabaseName: 'link-qa-botw-tenant'
          SqlUsername: '$(link-db_username)'
          SqlPassword: '$(link-db_pass)'
          deployType: 'SqlTask'
          SqlFile: '$(Pipeline.Workspace)/tenant-db/tenantDb.sql'

  ### Validation
  - job: UpdatevalidationSchema
    displayName: Update validation DB Schema
    steps:
      - download: current
        artifact: validation-db

      - task: SqlAzureDacpacDeployment@1
        displayName: Apply validation DB Schema
        inputs:
          azureSubscription: "NHSNLink Resource Manager"
          ServerName: '$(link-db_server)'
          DatabaseName: 'link-qa-botw-validation'
          SqlUsername: '$(link-db_username)'
          SqlPassword: '$(link-db_pass)'
          deployType: 'SqlTask'
          SqlFile: '$(Pipeline.Workspace)/validation-db/V20241101__Initial_schema.sql'