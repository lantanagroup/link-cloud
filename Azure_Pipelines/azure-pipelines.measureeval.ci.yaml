trigger:
  paths:
    include:
    - DotNet/MeasureEval/*
    - DotNet/Shared/*

pool:
  vmImage: 'ubuntu-latest'

variables:
  project: 'DotNet/MeasureEval/MeasureEval.csproj'
  testProject: 'DotNet/MeasureEvalUnitTests/MeasureEvalUnitTests.csproj'
  nugetPath: 'DotNet/MeasureEval/nuget.config'
  serviceName: 'MeasureEval'

steps:
  - template: ../azure-nuget-package-source.yml

  - task: DotNetCoreCLI@2
    displayName: Restore
    inputs:
      command: restore
      projects: '$(project)'
      nugetConfigPath: '$(nugetPath)'
      feedsToUse: config
  
  - task: DotNetCoreCLI@2
    displayName: Build
    inputs:
      command: build
      projects: '$(project)'

  - task: DotNetCoreCLI@2
    inputs:
      command: 'test'
      projects: $(testProject)
    displayName: 'Run Tests'

