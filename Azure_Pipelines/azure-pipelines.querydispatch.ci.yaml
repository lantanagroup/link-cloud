trigger:
  paths:
    include:
    - DotNet/QueryDispatch/*
    - DotNet/Shared/*

pool:
  vmImage: 'ubuntu-latest'

variables:
  project: 'DotNet/QueryDispatch/QueryDispatch.csproj'
  testProject: 'DotNet/QueryDispatchUnitTests/QueryDispatchUnitTests.csproj'
  nugetPath: 'DotNet/QueryDispatch/nuget.config'
  serviceName: 'QueryDispatch'

steps:
  - template: ../azure-nuget-package-source.yml

  - task: DotNetCoreCLI@2
    displayName: Restore
    inputs:
      command: restore
      projects: '$(project)'
      nugetConfigPath: '$(nugetPath)'
      feedsToUse: config
  
  - task: DotNetCoreCLI@2
    displayName: Build
    inputs:
      command: build
      projects: '$(project)'

  - task: DotNetCoreCLI@2
    inputs:
      command: 'test'
      projects: $(testProject)
    displayName: 'Run Tests'

